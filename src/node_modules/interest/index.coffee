View = require "view"
Collection = require "collection"

module.exports = View.extend
	props:
		name: "string"
	collections:
		keywords: Collection
	template: require "./template"
	bindings:
		name: hook: "name"
		keywords: hook: "keywords", type: require "bindings/collection/list"
