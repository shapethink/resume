module.exports =
	props:
		name: "string"
		label: "string"
		email: "string"
		picture: "string"
		phone: "string"
		website: "string"
		summary: "string"

	derived:
		mailto: deps: ["email"], fn: -> "mailto:#{@email}"

	children:
		location: require "location"

	collections:
		profiles: require("collection").of require "profile"
