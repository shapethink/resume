d3 = require "d3"
module.exports = (tag, render) ->
	(el, newVal, oldVal) ->
		data = d3.select(el).selectAll tag
			.data newVal.models

		data.exit().remove()
		data.enter().append tag

		data.call render
