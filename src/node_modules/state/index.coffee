State = require "ampersand-state"
module.exports = State.extend()
module.exports.for = (schema) ->
	view = require("view").for schema
	state = module.exports.extend(schema).extend
		view: view

	return state
